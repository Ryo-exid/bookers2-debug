<!--インスタンス変数は、favorites_controllerのcreateアクションで定義されたものが使用できます。-->
<!--html()以降では部分テンプレートを呼び出しています。-->
$("#favorite-btn_"+<%= @book.id %>).html("<%= j(render 'favorites/favorite-btn', book: @book) %>");